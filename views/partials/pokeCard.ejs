<section class="poke-card">
    <h2><%= `${pokemon.name}` %></h2>
    <a href="/details/pokemon/<%=pokemon.id%>">details</a>
    <a href="/update/pokemons/<%=pokemon.id%>">update</a>
    <form action="/delete/pokemon/<%=pokemon.id%>" method="post">
        <button type="submit" onclick="return confirm('Are you sure, you want to delete this pokemon?')">Delete</button>
    </form>
</section>